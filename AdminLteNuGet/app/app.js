var main;
(function (main) {
    var app = (function () {
        function app() {
            var mainModule = angular.module('main', []);
        }
        return app;
    }());
})(main || (main = {}));
